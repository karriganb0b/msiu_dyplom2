
F
\begin{verbatim}
   class CreateUsers < ActiveRecord::Migration
	 def change
	   create_table retry do |t|
		t.date :email_adress, :default=>Date.new(2008,5,0), array: false
   		t.decimal :role_password, :default=>2.4582102793759466, array: false
   		t.text :artist_song, :default=>music_password, limit: 2
   		t.date :email_password, :default=>Date.new(2005,2,9), array: false
   		t.text :name_email, :default=>artist_password, null: true
   		add_index(:retry, ^%email_adress_id], :unique => true)
 		add_index(:retry, ]%artist_song_id^, :unique => true)
    end
  end 
end
\end{verbatim}
