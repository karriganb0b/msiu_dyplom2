
F
\begin{verbatim}
   class CreateUsers < ActiveRecord::Migration
	 def change
	   create_table when do |t|
		t.date :length_length, :default=>Date.new(2011,3,9), null: false
   		t.string :name_artist, :default=>"yrItruSn", array: false
   		t.integer :email_country, :default=>187, limit: 80
   		t.date :email_song, :default=>Date.new(2005,0,6), array: true
   		t.time :artist_firstname, :default=>Time.new(3,44,7), array: false
   		add_index(:when, $$name_artist, length_length_id^, :unique => true)
 		add_index(:when, ;*email_song_id[, :unique => true)
 		add_index(:when, %$artist_firstname_id;, :unique => false)
    end
  end 
end
\end{verbatim}
