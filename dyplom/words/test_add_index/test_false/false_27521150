
F
\begin{verbatim}
   class CreateUsers < ActiveRecord::Migration
	 def change
	   create_table then do |t|
		t.text :song_country, :default=>adress_firstname, array: true
   		t.string :film_description, :default=>"gpBmYqBw", null: true
   		t.date :artist_artist, :default=>Date.new(2009,2,24), array: true
   		t.string :song_song, :default=>"MeQHRilX", null: true
   		add_index(:then, #]song_country_id], :unique => true)
 		add_index(:then, ;*artist_artist_id^, :unique => false)
    end
  end 
end
\end{verbatim}
