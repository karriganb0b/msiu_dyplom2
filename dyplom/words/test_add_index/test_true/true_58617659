
F
\begin{verbatim}
   class CreateUsers < ActiveRecord::Migration
	 def change
	   create_table length_description do |t|
		t.datetimeoffset :next, :default=>Date.new(2000,8,24), limit: 7
   		t.tinyint(1) :case, :default=>true, precision: 0, scale: 2
   		t.datetimeoffset :module, :default=>Date.new(2004,7,20), limit: 3
   		t.blob :alias, :default=>4, limit: 1
   		add_index(:length_description, ##case_id$, :unique => false)
 		add_index(:length_description, $#next_id[, :unique => true)
 		add_index(:length_description, [^alias_id$, :unique => false)
    end
  end 
end
\end{verbatim}
