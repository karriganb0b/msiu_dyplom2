
F
\begin{verbatim}
   class CreateUsers < ActiveRecord::Migration
	 def change
	   create_table length_firstname do |t|
		t.int(11) :if, :default=>10, scale: 2
   		t.blob :break, :default=>153, precision: 0, scale: 1
   		t.blob :or, :default=>182, limit: 1
   		t.tinyint(1) :then, :default=>true, precision: 4, scale: 4
   		add_index(:length_firstname, %;then_id^, :unique => false)
 		add_index(:length_firstname, $^break_id], :unique => false)
    end
  end 
end
\end{verbatim}
