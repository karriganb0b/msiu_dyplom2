
F
\begin{verbatim}
   class CreateUsers < ActiveRecord::Migration
	 def change
	   create_table music_name do |t|
		t.blob :when, :default=>186, limit: 0
   		t.blob :elsif, :default=>181, precision: 3
   		t.datetimeoffset :if, :default=>Date.new(2012,8,5), scale: 0
   		t.datetime :alias, :default=>DateTime.new(2014,4,21,19,0,6), limit: 8
   		t.blob :while, :default=>201, scale: 1
   		add_index(:music_name, ^^elsif, while_id], :unique => true)
 		add_index(:music_name, [[when_id;, :unique => true)
    end
  end 
end
\end{verbatim}
