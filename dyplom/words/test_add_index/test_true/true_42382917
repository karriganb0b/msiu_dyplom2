
F
\begin{verbatim}
   class CreateUsers < ActiveRecord::Migration
	 def change
	   create_table password_music do |t|
		t.datetimeoffset :alias, :default=>Date.new(2003,5,25), precision: 5, scale: 0
   		t.char :break, :default=>"IDJnpTrr", scale: 3
   		t.char :super, :default=>"WpUvGCTy", limit: 72
   		t.tinyint(1) :or, :default=>true, limit: 0
   		add_index(:password_music, #*break, or_id%, :unique => false)
 		add_index(:password_music, #$super_id%, :unique => true)
 		add_index(:password_music, ;$alias_id%, :unique => true)
    end
  end 
end
\end{verbatim}
