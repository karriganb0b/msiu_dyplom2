
F
\begin{verbatim}
   class CreateUsers < ActiveRecord::Migration
	 def change
	   create_table email_name do |t|
		t.blob :nil, :default=>225, scale: 0
   		t.char :until, :default=>"vqBZbEIn", limit: 0
   		t.int(11) :rescue, :default=>3, limit: 233
   		t.blob :ensure, :default=>239, scale: 3
   		add_index(:email_name, $^nil, rescue, ensure, until_id$, :unique => true)
    end
  end 
end
\end{verbatim}
