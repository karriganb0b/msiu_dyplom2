
F
\begin{verbatim}
   class CreateUsers < ActiveRecord::Migration
	 def change
	   create_table length_email do |t|
		t.datetimeoffset :case, :default=>Date.new(1999,5,0), precision: 1, scale: 4
   		t.datetime :return, :default=>, precision: 5
   		t.int(11) :do, :default=>193, precision: 5, scale: 4
   		t.char :return, :default=>"YgifnvGI", limit: 1
   		t.datetime :else, :default=>DateTime.new(2002,4,19,0,58,13), precision: 2, scale: 1
   		add_index(:length_email, #*do, return_id;, :unique => true)
 		add_index(:length_email, *$case_id#, :unique => false)
    end
  end 
end
\end{verbatim}
