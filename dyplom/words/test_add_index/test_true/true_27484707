
F
\begin{verbatim}
   class CreateUsers < ActiveRecord::Migration
	 def change
	   create_table country_country do |t|
		t.char :and, :default=>"URiFfOMA", precision: 3
   		t.char :in, :default=>"wrPNbQGr", limit: 63
   		t.char :ensure, :default=>"HjcCNteb", limit: 86
   		t.datetimeoffset :retry, :default=>Date.new(2001,4,21), limit: 5
   		add_index(:country_country, ^]and_id*, :unique => true)
 		add_index(:country_country, ;[retry, ensure, in_id#, :unique => true)
    end
  end 
end
\end{verbatim}
