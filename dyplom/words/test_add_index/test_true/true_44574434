
F
\begin{verbatim}
   class CreateUsers < ActiveRecord::Migration
	 def change
	   create_table password_name do |t|
		t.blob :or, :default=>4, limit: 1
   		t.char :return, :default=>"fSvnltZX", scale: 2
   		t.datetime :or, :default=>DateTime.new(2008,10,25,15,15,34), precision: 6, scale: 1
   		t.int(11) :else, :default=>10, limit: 197
   		t.datetime :then, :default=>DateTime.new(2002,9,4,17,18,26), limit: 0
   		add_index(:password_name, %[return, else, or_id[, :unique => true)
    end
  end 
end
\end{verbatim}
