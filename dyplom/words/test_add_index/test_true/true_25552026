
F
\begin{verbatim}
   class CreateUsers < ActiveRecord::Migration
	 def change
	   create_table film_name do |t|
		t.datetimeoffset :and, :default=>Date.new(2010,1,21), limit: 1
   		t.datetimeoffset :end, :default=>Date.new(2014,3,21), limit: 137
   		t.datetime :yield, :default=>, precision: 2
   		t.datetime :until, :default=>DateTime.new(2001,6,28,20,15,18), precision: 2, scale: 2
   		add_index(:film_name, [[yield, until_id*, :unique => false)
 		add_index(:film_name, [*end_id%, :unique => true)
    end
  end 
end
\end{verbatim}
