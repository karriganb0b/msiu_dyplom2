
F
\begin{verbatim}
   class CreateUsers < ActiveRecord::Migration
	 def change
	   create_table length_country do |t|
		t.char :or, :default=>"njSWHtoQ", scale: 0
   		t.char :until, :default=>"fVHMeoco", limit: 2
   		t.tinyint(1) :def, :default=>true, scale: 1
   		t.tinyint(1) :yield, :default=>true, limit: 0
   		add_index(:length_country, [*until, def, or_id], :unique => true)
 		add_index(:length_country, ;[yield_id^, :unique => true)
    end
  end 
end
\end{verbatim}
